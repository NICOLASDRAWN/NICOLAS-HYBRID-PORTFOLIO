/**
 * @deprecated — NO USADO.
 *
 * Este componente era una versión alternativa del hero que quedó huérfana
 * cuando se migró a `hero-futuristic.tsx` (ver `src/app/page.tsx`).
 *
 * Nadie lo importa en el codebase actual. Se conservó como archivo vacío
 * en vez de borrarse desde el sandbox por un tema de permisos, pero puedes
 * eliminarlo localmente antes del próximo push:
 *
 *     rm src/components/blocks/hero-section-9.tsx
 *
 * Si por cualquier razón lo vuelves a necesitar, está en el historial de git:
 *
 *     git log --all --full-history -- src/components/blocks/hero-section-9.tsx
 */
export {};
